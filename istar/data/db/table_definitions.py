CREATE_PLAYERS_CURRENT_SUMMARY = """
    CREATE TABLE IF NOT EXISTS players_current_summary (
        chance_of_playing_next_round         DOUBLE,
        chance_of_playing_this_round         DOUBLE,
        code                                 INTEGER,
        cost_change_event                    INTEGER,
        cost_change_event_fall               INTEGER,
        cost_change_start                    INTEGER,
        cost_change_start_fall               INTEGER,
        dreamteam_count                      INTEGER,
        element_type                         INTEGER,
        ep_next                              DOUBLE,
        ep_this                              DOUBLE,
        event_points                         INTEGER,
        first_name                           VARCHAR,
        form                                 DOUBLE,
        player_id                            INTEGER  PRIMARY KEY,
        in_dreamteam                         INTEGER,
        news                                 VARCHAR,
        news_added                           VARCHAR,
        now_cost                             INTEGER,
        photo                                VARCHAR,
        points_per_game                      DOUBLE,
        second_name                          VARCHAR,
        selected_by_percent                  DOUBLE,
        special                              INTEGER,
        squad_number                         DOUBLE,
        status                               VARCHAR,
        team                                 INTEGER,
        team_code                            INTEGER,
        total_points                         INTEGER,
        transfers_in                         INTEGER,
        transfers_in_event                   INTEGER,
        transfers_out                        INTEGER,
        transfers_out_event                  INTEGER,
        value_form                           DOUBLE,
        value_season                         DOUBLE,
        web_name                             VARCHAR,
        minutes                              INTEGER,
        goals_scored                         INTEGER,
        assists                              INTEGER,
        clean_sheets                         INTEGER,
        goals_conceded                       INTEGER,
        own_goals                            INTEGER,
        penalties_saved                      INTEGER,
        penalties_missed                     INTEGER,
        yellow_cards                         INTEGER,
        red_cards                            INTEGER,
        saves                                INTEGER,
        bonus                                INTEGER,
        bps                                  INTEGER,
        influence                            DOUBLE,
        creativity                           DOUBLE,
        threat                               DOUBLE,
        ict_index                            DOUBLE,
        influence_rank                       INTEGER,
        influence_rank_type                  INTEGER,
        creativity_rank                      INTEGER,
        creativity_rank_type                 INTEGER,
        threat_rank                          INTEGER,
        threat_rank_type                     INTEGER,
        ict_index_rank                       INTEGER,
        ict_index_rank_type                  INTEGER,
        corners_and_indirect_freekicks_order DOUBLE,
        corners_and_indirect_freekicks_text  DOUBLE,
        direct_freekicks_order               DOUBLE,
        direct_freekicks_text                DOUBLE,
        penalties_order                      DOUBLE,
        fpl_season                               VARCHAR
    );
"""

CREATE_PLAYERS_HISTORY = """
    CREATE TABLE IF NOT EXISTS players_history (
        player_id          INTEGER  PRIMARY KEY,
        fixture            INTEGER,
        opponent_team      INTEGER,
        total_points       INTEGER,
        was_home           INTEGER,
        kickoff_time       VARCHAR,
        team_h_score       INTEGER,
        team_a_score       INTEGER,
        round              INTEGER,
        minutes            INTEGER,
        goals_scored       INTEGER,
        assists            INTEGER,
        clean_sheets       INTEGER,
        goals_conceded     INTEGER,
        own_goals          INTEGER,
        penalties_saved    INTEGER,
        penalties_missed   INTEGER,
        yellow_cards       INTEGER,
        red_cards          INTEGER,
        saves              INTEGER,
        bonus              INTEGER,
        bps                INTEGER,
        influence          DOUBLE,
        creativity         DOUBLE,
        threat             DOUBLE,
        ict_index          DOUBLE,
        value              INTEGER,
        transfers_balance  INTEGER,
        selected           INTEGER,
        transfers_in       INTEGER,
        transfers_out      INTEGER,
        fpl_season         VARCHAR
    );
"""

CREATE_PLAYERS_FIXTURES = """
    CREATE TABLE IF NOT EXISTS players_fixtures (
        id                      INTEGER  PRIMARY KEY,
        code                    INTEGER,
        team_h                  INTEGER,
        team_h_score            DOUBLE,
        team_a                  INTEGER,
        team_a_score            DOUBLE,
        event                   DOUBLE,
        finished                INTEGER,
        minutes                 INTEGER,
        provisional_start_time  INTEGER,
        kickoff_time            VARCHAR,
        event_name              VARCHAR,
        is_home                 INTEGER,
        difficulty              INTEGER,
        player_id               INTEGER,
        fpl_season              VARCHAR
    );
"""

CREATE_PLAYERS_HISTORY_PAST = """
    CREATE TABLE IF NOT EXISTS players_history_past (
        season_name       VARCHAR,
        element_code      DOUBLE,
        start_cost        DOUBLE,
        end_cost          DOUBLE,
        total_points      DOUBLE,
        minutes           DOUBLE,
        goals_scored      DOUBLE,
        assists           DOUBLE,
        clean_sheets      DOUBLE,
        goals_conceded    DOUBLE,
        own_goals         DOUBLE,
        penalties_saved   DOUBLE,
        penalties_missed  DOUBLE,
        yellow_cards      DOUBLE,
        red_cards         DOUBLE,
        saves             DOUBLE,
        bonus             DOUBLE,
        bps               DOUBLE,
        influence         DOUBLE,
        creativity        DOUBLE,
        threat            DOUBLE,
        ict_index         DOUBLE,
        player_id         INTEGER PRIMARY KEY,
        fpl_season        VARCHAR
    );
"""

CREATE_TEAMS = """
    CREATE TABLE IF NOT EXISTS teams (
        code                  INTEGER,
        draw                  INTEGER,
        form                  DOUBLE,
        team_id               INTEGER  PRIMARY KEY,
        loss                  INTEGER,
        name                  VARCHAR,
        played                INTEGER,
        points                INTEGER,
        position              INTEGER,
        short_name            VARCHAR,
        strength              INTEGER,
        team_division         DOUBLE,
        unavailable           INTEGER,
        win                   INTEGER,
        strength_overall_home INTEGER,
        strength_overall_away INTEGER,
        strength_attack_home  INTEGER,
        strength_attack_away  INTEGER,
        strength_defence_home INTEGER,
        strength_defence_away INTEGER,
        pulse_id              INTEGER,
        fpl_season            VARCHAR
    );
"""
